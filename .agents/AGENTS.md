# Custom Agent Rules

- **Milestone Tracking:** Whenever a milestone is completed, ALWAYS update the `Build Status` table in `README.md` to change the corresponding status from `⬜` to `✅`. Do this proactively without waiting for the user to ask.
